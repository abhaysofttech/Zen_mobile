import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchByAssociatePage } from './search-by-associate.page';

describe('SearchByAssociatePage', () => {
  let component: SearchByAssociatePage;
  let fixture: ComponentFixture<SearchByAssociatePage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchByAssociatePage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchByAssociatePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
