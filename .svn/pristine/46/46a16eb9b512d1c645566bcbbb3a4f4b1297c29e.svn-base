import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EmployeeMoreInfoWithChartsPage } from './employee-more-info-with-charts.page';

describe('EmployeeMoreInfoWithChartsPage', () => {
  let component: EmployeeMoreInfoWithChartsPage;
  let fixture: ComponentFixture<EmployeeMoreInfoWithChartsPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EmployeeMoreInfoWithChartsPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EmployeeMoreInfoWithChartsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
