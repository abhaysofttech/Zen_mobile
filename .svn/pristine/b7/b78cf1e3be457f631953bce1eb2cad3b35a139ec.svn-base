import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PgmDistributionPage } from './pgm-distribution.page';

describe('PgmDistributionPage', () => {
  let component: PgmDistributionPage;
  let fixture: ComponentFixture<PgmDistributionPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PgmDistributionPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PgmDistributionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
