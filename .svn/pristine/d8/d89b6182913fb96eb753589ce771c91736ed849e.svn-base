import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OverallAssociatesDrilldownTwoPage } from './overall-associates-drilldown-two.page';

describe('OverallAssociatesDrilldownTwoPage', () => {
  let component: OverallAssociatesDrilldownTwoPage;
  let fixture: ComponentFixture<OverallAssociatesDrilldownTwoPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OverallAssociatesDrilldownTwoPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OverallAssociatesDrilldownTwoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
