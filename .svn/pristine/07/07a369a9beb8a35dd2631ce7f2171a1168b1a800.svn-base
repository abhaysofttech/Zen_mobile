import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EmployeeHistoryRatingsDrilldownPage } from './employee-history-ratings-drilldown.page';

describe('EmployeeHistoryRatingsDrilldownPage', () => {
  let component: EmployeeHistoryRatingsDrilldownPage;
  let fixture: ComponentFixture<EmployeeHistoryRatingsDrilldownPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EmployeeHistoryRatingsDrilldownPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EmployeeHistoryRatingsDrilldownPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
