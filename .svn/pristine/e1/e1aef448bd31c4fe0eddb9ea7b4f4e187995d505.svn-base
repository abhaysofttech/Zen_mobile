import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RwfManagementPage } from './rwf-management.page';

describe('RwfManagementPage', () => {
  let component: RwfManagementPage;
  let fixture: ComponentFixture<RwfManagementPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RwfManagementPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RwfManagementPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
