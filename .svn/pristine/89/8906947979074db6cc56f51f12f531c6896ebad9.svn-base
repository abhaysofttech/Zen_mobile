import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OverallAssociatesMoreDetailsDrilldownPage } from './overall-associates-more-details-drilldown.page';

describe('OverallAssociatesMoreDetailsDrilldownPage', () => {
  let component: OverallAssociatesMoreDetailsDrilldownPage;
  let fixture: ComponentFixture<OverallAssociatesMoreDetailsDrilldownPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OverallAssociatesMoreDetailsDrilldownPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OverallAssociatesMoreDetailsDrilldownPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
